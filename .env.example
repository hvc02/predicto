# Database (Supabase PostgreSQL)
# From Supabase: Project Settings → Database → Connection string (URI)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# NextAuth v5 (use https in production)
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# Admin: comma-separated emails that can create/resolve markets
ADMIN_EMAILS="admin@example.com"

# Razorpay (Wallet deposits). Get from Dashboard → Settings → API Keys (test/live)
RAZORPAY_KEY_ID="rzp_test_..."
RAZORPAY_KEY_SECRET="..."
# Webhook secret from Dashboard → Webhooks → Create (e.g. payment.captured)
RAZORPAY_WEBHOOK_SECRET="..."
# Publishable key for checkout (same as Key Id or use separate if needed)
NEXT_PUBLIC_RAZORPAY_KEY_ID="rzp_test_..."
